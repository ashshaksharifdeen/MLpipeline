{
    "type": "node",
  "request": "launch",
  "name": "Launch Program",
  "program": "${workspaceFolder}/app.js",
  "cwd": "${workspaceFolder}",
  "args": ["${env:USERNAME}"],
  "terminal.integrated.env.osx": {"PYTHONPATH": "${workspaceFolder}"}
}